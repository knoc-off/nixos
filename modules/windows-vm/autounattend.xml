<?xml version="1.0" encoding="utf-8"?>
<!-- Minimal skeleton for unattended Windows 11 VM installation.
     All VM-critical elements (locale, disk layout, edition, hardware
     bypasses, VirtIO drivers, user account, OOBE settings, post-install
     script) are ensured by patch-autounattend.py at build time.

     You can replace this file with any autounattend.xml (e.g.
     UnattendedWinstall) via the autounattendFile option - the patcher
     will merge VM automation on top of whatever base you provide. -->
<unattend xmlns="urn:schemas-microsoft-com:unattend"
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
</unattend>
